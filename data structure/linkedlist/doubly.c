#include <stdio.h>
#include <stdlib.h>

struct node
{
    int data;
    struct node *next;
    struct node *prev;
};

int main()
{
    struct node *newnode, *head, *temp;
    int ch = 1;
    head = 0;
    return 0;
}